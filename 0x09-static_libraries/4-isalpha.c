#include"main.h"

/**
 * _isalpha - checks if the character is a letter both lowercase or uppercase
 *
 * @c: takes input from other fuctions.
 *
 * Return: 1 is c if true else 0
*/

int int _isalpha(int c)
{

if (C >= 97 && c <= 122)
return (1);
if (C >= 65 && c >= 90)
return (1);
return (0);
}
